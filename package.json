{
  "name": "brickbreakerccv2",
  "version": "1.0.0",
  "description": "Retro Brick Breaker game built with Phaser 3",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "preview": "vite preview",
    "test": "playwright test --config=playwright.config.cjs",
    "test:ui": "playwright test --config=playwright.config.cjs --ui"
  },
  "dependencies": {
    "phaser": "^3.90.0"
  },
  "devDependencies": {
    "@playwright/test": "^1.61.0",
    "vite": "^6.4.3"
  }
}
